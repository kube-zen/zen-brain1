---
# DEPRECATED: This CRD uses group zen.kube-zen.com.
# Migrating to brain.zen-mesh.io or platform.zen-mesh.io. See docs/01-ARCHITECTURE/ADR/0010_API_GROUP_MIGRATION.md.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: brainpolicies.zen.kube-zen.com
spec:
  group: zen.kube-zen.com
  names:
    kind: BrainPolicy
    listKind: BrainPolicyList
    plural: brainpolicies
    shortNames:
    - bp
    singular: brainpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BrainPolicy is a cluster-scoped policy for the Factory (Block
          4).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BrainPolicySpec defines the desired state of the policy.
            properties:
              budgetLimitUSD:
                description: BudgetLimitUSD is the default per-project budget cap
                  in USD (0 = no cap).
                minimum: 0
                type: number
              defaultApprovalLevel:
                description: DefaultApprovalLevel is the default approval level when
                  RequiresApproval is true.
                minLength: 1
                type: string
              rules:
                description: Rules are policy rules (e.g. require approval for execute_task
                  above cost threshold).
                items:
                  description: PolicyRuleSpec defines a single policy rule.
                  properties:
                    action:
                      description: Action is the action this rule applies to (e.g.
                        execute_task, call_llm).
                      minLength: 1
                      type: string
                    allowedModels:
                      description: AllowedModels restricts to these model IDs when
                        set (empty = all).
                      items:
                        type: string
                      type: array
                    maxCostUSD:
                      description: MaxCostUSD is the max cost in USD for a single
                        call (0 = no limit).
                      minimum: 0
                      type: number
                    name:
                      description: Name is a short name for the rule.
                      minLength: 1
                      type: string
                    requiresApproval:
                      description: RequiresApproval when true requires human approval
                        before the action.
                      type: boolean
                  required:
                  - action
                  - name
                  type: object
                type: array
            type: object
          status:
            description: BrainPolicyStatus defines the observed state of BrainPolicy.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              message:
                type: string
              phase:
                description: BrainPolicyPhase is the phase of a BrainPolicy.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
