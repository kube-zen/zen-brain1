# Binaries — root-level build outputs
/factory-fill
/remediation-worker
/queue-steward
/roadmap-steward
/useful-batch
/scheduler
/apiserver
/foreman
/zen-brain
/controller
/create-jira-issues
/zen-brain1
/mlq-dispatcher
/metrics-report
/run-mlq-rescue
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Binaries — cmd/ build outputs (mirror of root)
cmd/factory-fill/factory-fill
cmd/queue-steward/queue-steward
cmd/roadmap-steward/roadmap-steward
cmd/remediation-worker/remediation-worker
cmd/useful-batch/useful-batch
cmd/scheduler/scheduler
cmd/finding-ticketizer/finding-ticketizer
cmd/admission-gate/admission-gate
cmd/jira-ledger/jira-ledger
cmd/create-jira-issues/create-jira-issues
cmd/mlq-dispatcher/mlq-dispatcher
cmd/metrics-report/metrics-report
cmd/run-mlq-rescue/run-mlq-rescue

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool (canonical path: .artifacts/coverage/)
*.out
coverage.out
.artifacts/

# Dependency directories (remove the comment below to include it)
vendor/

# Go workspace file
go.work
go.work.sum

# Editor and IDE directories
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Kubernetes generated files
kustomization.yaml
kustomize/

# Local development configuration
config.dev.local.yaml
*.local

# Zen-Brain home directory (should not be committed)
# This is created at runtime based on ZEN_BRAIN_HOME
# The structure documentation is kept in README.md

# Session persistence (SQLite)
data/

# Temporary and cache directories
tmp/
.cache/
.env
.env.local
.env.*.local

# Jira credentials (ZenLock bridge file for host runtime)
.zen-brain/jira-credentials.env

# Additional credential file patterns (ZB-013)
**/secrets/jira.yaml
**/.env.jira*
**/jira-input.yaml
**/jira-credentials.env
**/.zen-lock/*.key

# Log files
*.log
logs/

# Zen-Brain runtime directories (do not ignore cmd/zen-brain source)
.zen/
/zen-brain

# Temporary test binaries and scripts
test_zencontext*

# Scratch / one-off scripts and backups (do not commit)
*.backup
*.broken
*.clean
apply_template_*.py
fix_factory.py
remove_bounded.py
template_manager.patch

# Archive artifacts (do not commit)
*.tar.gz

# Python cache
__pycache__/
*.pyc

# Zen-Lock credentials (NEVER COMMIT)
DONOTASKMOREFORTHISSHIT.txt
ZENBRAIN*NEVERDELETETHISSHIT.age
zen-lock-private-key.age
zen-lock-public-key.age

# Vendored sqlite generated Go files (restore via go mod vendor)
vendor/modernc.org/sqlite/lib/

# Auto-generated runtime evidence (factory-fill dashboard)
docs/05-OPERATIONS/evidence/factory-fill-and-backlog-utilization.md
