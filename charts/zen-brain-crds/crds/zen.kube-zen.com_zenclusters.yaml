---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: zenclusters.zen.kube-zen.com
spec:
  group: zen.kube-zen.com
  names:
    kind: ZenCluster
    listKind: ZenClusterList
    plural: zenclusters
    singular: zencluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.endpoint
      name: Endpoint
      type: string
    - jsonPath: .spec.location
      name: Location
      type: string
    - jsonPath: .spec.status
      name: Status
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZenCluster is the Schema for the zenclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZenClusterSpec defines the desired state of ZenCluster
            properties:
              auth_ref:
                description: AuthRef is a reference to a Kubernetes Secret containing
                  kubeconfig
                minLength: 1
                type: string
              capacity:
                description: Capacity defines the cluster's resource capacity
                properties:
                  cpu_cores:
                    description: CPUCores is the total CPU cores available
                    format: int64
                    minimum: 0
                    type: integer
                  gpus:
                    description: GPUs is the number of GPUs available
                    format: int64
                    minimum: 0
                    type: integer
                  memory_gb:
                    description: MemoryGB is the total memory in GB available
                    format: int64
                    minimum: 0
                    type: integer
                  storage_gb:
                    description: StorageGB is the total storage in GB available
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              endpoint:
                description: Endpoint is the Kubernetes API server endpoint
                minLength: 1
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels are key-value pairs for cluster selection
                type: object
              location:
                description: Location indicates where the cluster is located (local,
                  cloud, edge)
                type: string
              metadata:
                additionalProperties:
                  type: string
                description: Metadata contains additional cluster metadata
                type: object
              status:
                description: Status indicates whether the cluster is active, inactive,
                  or draining
                type: string
            required:
            - auth_ref
            - endpoint
            type: object
          status:
            description: ZenClusterStatus defines the observed state of ZenCluster
            properties:
              available_capacity:
                description: AvailableCapacity is the currently available capacity
                properties:
                  cpu_cores:
                    description: CPUCores is the total CPU cores available
                    format: int64
                    minimum: 0
                    type: integer
                  gpus:
                    description: GPUs is the number of GPUs available
                    format: int64
                    minimum: 0
                    type: integer
                  memory_gb:
                    description: MemoryGB is the total memory in GB available
                    format: int64
                    minimum: 0
                    type: integer
                  storage_gb:
                    description: StorageGB is the total storage in GB available
                    format: int64
                    minimum: 0
                    type: integer
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the cluster's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              last_heartbeat_time:
                description: LastHeartbeatTime is the last time a heartbeat was received
                format: date-time
                type: string
              node_count:
                description: NodeCount is the number of nodes in the cluster
                format: int32
                type: integer
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled
                format: int64
                type: integer
              phase:
                description: Phase is the current phase of the cluster
                type: string
              version:
                description: Version is the Kubernetes version
                type: string
              zen_brain_agent_version:
                description: ZenBrainAgentVersion is the version of the zen-brain-agent
                  running on this cluster
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
