---
# DEPRECATED: This CRD uses group zen.kube-zen.com.
# Migrating to brain.zen-mesh.io or platform.zen-mesh.io. See docs/01-ARCHITECTURE/ADR/0010_API_GROUP_MIGRATION.md.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: zenprojects.zen.kube-zen.com
spec:
  group: zen.kube-zen.com
  names:
    kind: ZenProject
    listKind: ZenProjectList
    plural: zenprojects
    singular: zenproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.display_name
      name: Display Name
      type: string
    - jsonPath: .spec.cluster_ref
      name: Cluster
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.cost_spent_usd
      name: Cost
      type: number
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ZenProject is the Schema for the zenprojects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZenProjectSpec defines the desired state of ZenProject
            properties:
              auto_generate_funding_reports:
                description: AutoGenerateFundingReports indicates whether funding
                  reports should be auto-generated
                type: boolean
              cluster_ref:
                description: ClusterRef is the name of the ZenCluster where this project
                  runs
                minLength: 1
                type: string
              cost_budget_usd:
                description: CostBudgetUSD is the monthly budget limit in USD
                minimum: 0
                type: number
              display_name:
                description: DisplayName is the human-readable name of the project
                minLength: 1
                type: string
              funding_programs:
                description: FundingPrograms are the funding programs this project
                  participates in
                items:
                  type: string
                type: array
              kb_scopes:
                description: |-
                  KBScopes are the knowledge base scopes for this project
                  Example: ["zen-brain", "general", "company"]
                items:
                  type: string
                type: array
              metadata:
                additionalProperties:
                  type: string
                description: Metadata contains additional project metadata
                type: object
              repo_urls:
                description: RepoURLs are the Git repository URLs for this project
                items:
                  type: string
                type: array
              sred_disabled:
                description: SREDDisabled indicates whether SR&ED evidence collection
                  is disabled for this project
                type: boolean
              sred_tags:
                description: SREDTags are the SR&ED uncertainty categories for this
                  project
                items:
                  description: SREDTag represents SR&ED uncertainty categories.
                  type: string
                type: array
            required:
            - cluster_ref
            - display_name
            type: object
          status:
            description: ZenProjectStatus defines the observed state of ZenProject
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the project's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              cost_spent_usd:
                description: CostSpentUSD is the total cost spent this month
                type: number
              last_sync_time:
                description: LastSyncTime is the last time the project was synced
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation last reconciled
                format: int64
                type: integer
              phase:
                description: Phase is the current phase of the project
                type: string
              session_count:
                description: SessionCount is the total number of active sessions
                format: int64
                type: integer
              task_count:
                description: TaskCount is the total number of tasks executed
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
