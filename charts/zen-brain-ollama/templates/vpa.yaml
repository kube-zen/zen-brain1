# DEPRECATED (2026-03-26): This chart is for optional in-cluster Ollama. Primary runtime is llama.cpp (L1/L2). Ollama is L0 fallback via host Docker only. See docs/03-DESIGN/LOCAL_LLM_ESCALATION_LADDER.md.

{{- if and .Values.enabled .Values.vpa.enabled }}
# VPA: updateMode Initial = apply recommendations at pod creation only (no in-place resize).
# Requires VPA CRD + Metrics Server installed in cluster.
---
apiVersion: autoscaling.k8s.io/v1
kind: VerticalPodAutoscaler
metadata:
  name: ollama
  namespace: zen-brain
  labels:
    app: ollama
    app.kubernetes.io/part-of: zen-brain
spec:
  targetRef:
    apiVersion: apps/v1
    kind: StatefulSet
    name: ollama
  updatePolicy:
    updateMode: {{ .Values.vpa.updateMode | default "Initial" }}
  resourcePolicy:
    containerPolicies:
      - containerName: ollama
        minAllowed:
          {{- toYaml .Values.vpa.minAllowed | nindent 10 }}
        maxAllowed:
          {{- toYaml .Values.vpa.maxAllowed | nindent 10 }}
{{- end }}
