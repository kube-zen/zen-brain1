apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: zenlocks.security.kube-zen.io
spec:
  group: security.kube-zen.io
  names:
    plural: zenlocks
    singular: zenlock
    shortNames:
      plural: zenlocks
      singular: zenlock
    scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion:
              type: string
              enum: [security.kube-zen.io/v1alpha1]
            kind:
              type: string
              enum: [ZenLock]
            metadata:
              type: object
              properties:
                name:
                  type: string
                  maxLength: 63
                namespace:
                  type: string
                  maxLength: 63
            spec:
              type: object
              properties:
                algorithm:
                  type: string
                  enum: [age]
                encryptedData:
                  type: object
                  additionalProperties:
                    additionalProperties: false
                    patternProperties:
                      type: object
                      properties:
                        algorithm:
                          type: string
                          pattern: "^[A-Za-z0-9]+$"
                        data:
                          type: string
                  allowedSubjects:
                  type: array
                  items:
                    type: object
                    properties:
                      kind:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                  status:
                      type: object
                      properties:
                        conditions:
                          type: array
                          items:
                            properties:
                              lastTransitionTime:
                                type: string
                                format: date-time
                              message:
                                type: string
                              reason:
                                type: string
                              status:
                                type: string
                                enum: ["True", "False", "Unknown"]
                        type: array
                        phase:
                          type: string
                          enum: [Ready, Error]
                  finalizers:
                    type: array
                    items:
                      type: string
