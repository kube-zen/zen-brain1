# Hourly scan: fast triage tasks
name: hourly-scan
cadence: hourly
description: Quick defect, bug, and stub scan every hour
tasks: [defects, bug_hunting, stub_hunting]
workers: 5  # Conservative — 3-task batch, W=5 proven sufficient (W=7 showed no throughput gain)
