# Queue Steward Summary Cycle
# Cadence: every 30 minutes
# Purpose: queue-health summary artifact, composition review

name: queue-steward-summary
cadence: custom
interval_minutes: 30
description: |
  Queue-health summary cycle. Produces structured queue-health artifact,
  reviews backlog composition, tracks fill ratio trends.
tasks: [queue_steward]
workers: 1
steward_mode: summary
