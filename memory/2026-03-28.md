# 2026-03-28 Session Memory

## zen-brain1 Operations — Major Session

### What was done (in order):
1. **Phase A: Quality-gate terminal-state fix** — replaced fragile stdout string matching with explicit JSON terminal result files (`RESULT_DIR/{KEY}.json`). Worker writes terminal classification after every ticket. Factory-fill reads files for state decisions. Live-proven across 3 paths: done (ZB-1032, 17/25), needs_review (ZB-1037, 15/25), rejected (ZB-1045, 5/25→PAUSED). Commit: `9d0a2bf`.

2. **Phase B: Per-schedule workers override** — already existed in scheduler code (lines 224-236 of `cmd/scheduler/main.go`). Cleaned YAML config to single `workers:` per schedule. hourly=5, quad-hourly=5, daily=7. Commit: `bee1a89`.

3. **Phase C: Concurrency sweep W=5 vs W=7** — W=5 baseline: 13 tickets, 4 cycles, ~7 min, 6 done, 7 paused/blocked, 0 stuck. W=7: only 3 ready tickets (insufficient for proper comparison). No regression at W=7 but benefit unproven. Need ≥10 ready tickets. W=10 not approved. Commit: `f8086a2`.

4. **Phase D: Backlog priority / discovery throttle** — already existed in code (`countBacklogTickets()`, threshold > 10). Validated behavior. Policy: 70% remediation, 20% roadmap, 10% discovery. Commit: `91769e0`.

5. **Queue Steward** — new L1 factory-floor supervisor binary (`cmd/queue-steward/main.go`). Gathers Jira snapshot → calls L1 for structured JSON recommendations → executes safe actions → writes artifacts. Heuristic fallback when L1 down. Pilot: 3 runs, 3 tickets re-dispatched, 0 unsafe transitions. Commit: `ef0f009`.

6. **Session report** — written to `docs/05-OPERATIONS/evidence/2026-03-28-session-report.md`. Commit: `20ba3a2`.

### Key architecture decisions:
- Terminal result files (`RESULT_DIR/{KEY}.json`) are now the authoritative source for state transitions — stdout heuristics are fallback only
- Queue Steward manages flow, not strategy — GLM-5 stays in supervisor/policy role
- Underfilled factory with ready backlog is a bug
- W=7 is the only approved concurrency step; W=10 unapproved
- Worker-count changes are evidence-based, not guesswork
- Backlog drain remains the priority over new discovery

### Key files:
- `cmd/remediation-worker/main.go` — terminal result writing
- `cmd/factory-fill/main.go` — terminal result reading, dispatch, reconcile
- `cmd/scheduler/main.go` — per-schedule WORKERS override (lines 224-236), discovery throttle (lines 295-315, 415-445)
- `cmd/queue-steward/main.go` — new binary (666 lines Go)
- `config/task-templates/queue-steward-l1.yaml` — task template
- `config/schedules/queue-steward-fast.yaml` — 5 min cadence
- `config/schedules/queue-steward-summary.yaml` — 30 min cadence
- `config/schedules/daily-sweep.yaml` — workers: 7
- `config/schedules/hourly-scan.yaml` — workers: 5

### Jira state at end of session:
- Backlog: 53, In Progress: 3, Done: 677, PAUSED: 8, RETRYING: 0
- 677/741 = 91.4% Done rate
- 0 tickets stuck In Progress

### Evidence paths:
- `docs/05-OPERATIONS/evidence/phase-a-terminal-state-proof.md`
- `docs/05-OPERATIONS/evidence/phase-b-existing-capabilities.md`
- `docs/05-OPERATIONS/evidence/l1-workers-5-to-7-step.md`
- `docs/05-OPERATIONS/evidence/queue-steward-pilot.md`
- `docs/05-OPERATIONS/evidence/2026-03-28-session-report.md`
- `docs/05-OPERATIONS/QUEUE_STEWARD_ROLE.md`
- `/var/lib/zen-brain1/evidence/queue-steward/queue-health.json`

### User preferences observed:
- Wants strict phase ordering (A→B→C→D)
- Does not want re-implementation of things that already exist
- Wants checkpoint format after each commit
- Wants evidence-based decisions, not guesswork
- Sends very long, detailed instructions — follow them precisely
- Gets frustrated when assistant re-does work or loops
- Prefers concise status updates over verbose analysis
- Uses "status update pls" and "progress?" for check-ins

### Open items for next session:
- Activate queue-steward-fast in production scheduler
- Run proper W=5 vs W=7 comparison when backlog has ≥10 ready tickets
- Review queue-health artifacts after 24h of steward operation
