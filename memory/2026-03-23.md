# 2026-03-23 Memory Log

## MLQ Rescue Project (ZB-MLQ-RESCUE)

### Architecture Improvements
- **MLQ Selection Layer Created**: `internal/mlq/selector.go`
  - Backend type: provider, model, baseURL, timeout, thinking
  - SelectionCriteria: PreferredProvider, MaxContextTokens, RequireTools, TimeoutProfile
  - MLQ selector with RegisterBackend/Select methods
  - Default backend: ollama/qwen3.5:0.8b

- **Factory Runner Wired Through MLQ**: `internal/foreman/factory_runner.go`
  - Factory now uses MLQ for provider selection
  - Logs show: "[MLQ] Selected default backend: ollama (provider=ollama model=qwen3.5:0.8b)"
  - Backend selection is MLQ concern, not ad-hoc factory logic
  - Current Ollama path unchanged behavior

- **Llama.cpp Provider Ready**: `internal/llm/llama_provider.go`
  - Compiles cleanly
  - Implements llm.Provider interface
  - Deferred integration until MLQ fully wired
  - Ready as second backend under MLQ

### YAML Task Templates Created
- Created `config/task-templates/` directory
- Templates: rescue-mlq-from-0.1.yaml, rescue-yaml-roles-from-0.1.yaml, rescue-yaml-task-templates-from-0.1.yaml, rescue-scheduling-from-0.1.yaml
- Each template includes: summary, description, labels, work_type, allowed_paths, acceptance_criteria, verification, timeout_profile

### Generic Scheduling Started
- Created `internal/scheduler/types.go`
  - Schedule type: name, type (recurring/cron), interval, time_window, retry_policy
  - TimeWindow: start_hour, end_hour, days, timezone
  - RetryPolicy: max_retries, backoff, catch_up
- Created `config/schedules/nightshift.yaml` as example
- Generic scheduling model, not hardcoded "nightshift" product logic

### Live Loop Status
- ✅ Repo compiles cleanly
- ✅ Build pushed to zen-registry:5000/zen-brain:dev
- ✅ 5 workers active
- ✅ Ollama/qwen3.5:0.8b backend selected via MLQ
- ✅ 2700s timeout on normal lane (45 minutes)
- ✅ Jira → BrainTask → Foreman → Jira feedback loop healthy

### Key Decisions
1. **MLQ First**: Wire MLQ selection before adding llama.cpp backend
2. **Keep Current Path Working**: Ollama/qwen3.5:0.8b path must remain stable
3. **Generic Scheduling**: Use YAML schedules, not hardcoded product logic
4. **Template-Driven Dogfeed**: Real bounded work from templates, not fake artifacts

### 0.1 Rescue Backlog
- rescue-mlq-from-0.1
- rescue-yaml-roles-from-0.1
- rescue-yaml-task-templates-from-0.1
- rescue-scheduling-from-0.1

### Runtime Health
- Build: zen-registry:5000/zen-brain:dev
- Workers: 5 round-robin goroutines
- Model: qwen3.5:0.8b (via MLQ selection)
- Timeout: 2700s (45 minutes) for normal lane
- Keep-alive: 45m
- Short timeout: 60s for controlled failure tasks only

### Architecture Rules Established
1. MLQ is abstraction for backend selection
2. Ollama/qwen3.5:0.8b and llama.cpp/Q4 are backends under MLQ
3. Scheduling must be generic (YAML-defined, not hardcoded)
4. Task creation must be template-driven
5. Dogfeed = use system to improve system (real Jira work)
6. No fake self-referential artifacts
7. No "nightshift" as product architecture - just a schedule

### Files Modified
- internal/mlq/selector.go (new)
- internal/llm/llama_provider.go (new)
- internal/scheduler/types.go (new)
- internal/foreman/factory_runner.go (MLQ wired)
- config/task-templates/*.yaml (4 templates)
- config/schedules/nightshift.yaml (example)

### Next Session Priorities
1. Verify MLQ selection in runtime logs
2. Create 0.1 rescue Jira issues
3. Ingest rescue issues as BrainTasks
4. Start executing rescue backlog through working runtime
5. Add llama.cpp backend under MLQ (after MLQ proven)
