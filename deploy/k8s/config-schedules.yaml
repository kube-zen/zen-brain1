apiVersion: v1
kind: ConfigMap
metadata:
  name: schedules
  namespace: zen-brain
data:
  # NOTE: Scheduler reads from /config/schedules/*.yaml
  # This ConfigMap is mounted at /config/schedules
  # For now, we mount the host config directory via hostPath
  # Future: migrate schedule definitions here
  placeholder: |
    # Schedules are loaded from the mounted config volume
    # See ~/zen/zen-brain1/config/schedules/*.yaml on host
